# 2026-03-20 学习打卡

## 基本信息

- 周次：第 1 周
- 日期：2026-03-20
- 学习阶段：第 1 个月，第 1 周
- 今日主题：用命令行、curl 和 Git 调试你的 FastAPI 项目
- 预计投入：4-6 小时
- 建议练习目录：继续使用 `day4-fastapi-structure/`

## 今日目标

- [ ] 熟悉最基础的 Linux 文件查看命令
- [ ] 学会用 `curl` 调试 GET 和 POST 接口
- [ ] 学会用命令行查看代码和项目结构
- [ ] 学会用 Git 查看改动和提交历史

## 今日打卡表

| 模块 | 今日任务 | 学会标准 | 完成情况 | 结果记录 |
| --- | --- | --- | --- | --- |
| Linux 基础 | 用 `pwd`、`ls`、`cd`、`find` 查看目录和文件 | 能快速定位一个项目里的代码文件 | 未开始 | 待记录 |
| 文件查看 | 用 `cat` 和 `sed` 查看代码内容 | 不打开编辑器也能快速读代码 | 未开始 | 待记录 |
| API 调试 | 用 `curl` 调试 GET、POST 请求 | 能看懂返回的 JSON 是否符合预期 | 未开始 | 待记录 |
| Git | 用 `status`、`diff`、`log` 看改动和历史 | 能知道自己今天改了什么 | 未开始 | 待记录 |

## 今日实操命令路线

### 1. 定位到仓库根目录

```bash
cd /Users/ace9264mac/Documents/personal/aia
pwd
ls -la
```

### 2. 查看当前项目结构

```bash
find . -maxdepth 2 -type d | sort
find day4-fastapi-structure -maxdepth 3 -type f | sort
```

### 3. 用命令查看代码

```bash
sed -n '1,200p' day4-fastapi-structure/app/main.py
sed -n '1,200p' day4-fastapi-structure/app/routers/health.py
sed -n '1,200p' day4-fastapi-structure/app/routers/users.py
cat day4-fastapi-structure/requirements.txt
```

### 4. 启动昨天的项目

```bash
cd /Users/ace9264mac/Documents/personal/aia/day4-fastapi-structure
source .venv/bin/activate
APP_NAME="debug day" uvicorn app.main:app --reload
```

### 5. 新开终端调试接口

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/users/
curl -X POST http://127.0.0.1:8000/users/ \
  -H "Content-Type: application/json" \
  -d '{"name":"DebugUser"}'
curl http://127.0.0.1:8000/users/
```

### 6. 回到根目录查看 Git 状态

```bash
cd /Users/ace9264mac/Documents/personal/aia
git status
git diff
git log --oneline -5
```

### 7. 完成后提交

```bash
git add .
git commit -m "docs: add week1 day5 learning plan"
```

## 今天学到什么程度算学会

1. 你能不用图形界面就找到项目代码。
2. 你能用 `sed` 或 `cat` 快速看文件内容。
3. 你能用 `curl` 发请求并判断结果对不对。
4. 你能用 Git 快速知道今天自己改了哪些内容。

## 今日产出

- 一次完整的命令行查看项目练习
- 一次用 `curl` 调试接口的练习
- 一次用 Git 追踪改动的练习

## 今日复盘

### 今天学会了什么

- 待记录

### 还不熟的地方

- 待记录

### 明天要继续什么

- 明天开始做本周的第一个小型实战项目：Todo API
