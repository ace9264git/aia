# 2026-03-16 学习打卡

## 基本信息

- 周次：第 1 周
- 日期：2026-03-16
- 学习阶段：第 1 个月，第 1 周
- 今日主题：Python + Web 基础入门，完成 FastAPI 第一个可运行服务
- 预计投入：4-6 小时

## 今日目标

- [ ] 搭好今天学习需要的 Python 和 FastAPI 基础环境
- [ ] 理解 HTTP / JSON / API 请求响应的最核心概念
- [ ] 写出一个可以运行的 FastAPI Hello World 服务
- [ ] 完成至少 1 次 Git 提交并记录今天的学习内容

## 今日学习节奏

| 时间段 | 学习安排 |
| --- | --- |
| 前 2 小时 | Python 环境、虚拟环境、HTTP/JSON 基础、FastAPI 基础概念 |
| 中间 2 小时 | 写 FastAPI Hello World、增加一个简单接口、启动并测试 |
| 最后 1-2 小时 | 整理笔记、提交 Git、更新学习记录 |

## 今日实操命令路线

按下面顺序执行即可，今天的目标是搭好环境、跑通 FastAPI、完成一次 Git 提交。

### 1. 创建今天的项目目录

```bash
mkdir -p projects/basic-agent/day1-fastapi
cd projects/basic-agent/day1-fastapi
```

学会标准：

- 知道今天的代码应该放在哪里
- 能自己进入正确的项目目录开始学习

### 2. 确认 Python 版本

```bash
python3 --version
```

学会标准：

- 知道本机是否已经有可用的 Python 3 环境

### 3. 创建虚拟环境

```bash
python3 -m venv .venv
```

学会标准：

- 知道虚拟环境是项目级隔离环境
- 能独立为一个 Python 项目创建 `.venv`

### 4. 激活虚拟环境

```bash
source .venv/bin/activate
```

学会标准：

- 能看懂命令行前面出现的 `(.venv)`
- 知道以后装依赖前要先激活虚拟环境

### 5. 安装 FastAPI 和 Uvicorn

```bash
pip install fastapi uvicorn
```

学会标准：

- 知道 `fastapi` 是框架，`uvicorn` 是运行服务的服务器

### 6. 生成依赖文件

```bash
pip freeze > requirements.txt
```

学会标准：

- 知道 Python 项目的依赖需要被记录下来
- 知道 `requirements.txt` 是最基础的依赖清单

### 7. 创建应用文件

先新建 `main.py`，然后写入下面内容：

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()


class MessageRequest(BaseModel):
    text: str


@app.get("/")
def read_root():
    return {"message": "Hello, all in ai"}


@app.get("/health")
def health_check():
    return {"status": "ok"}


@app.post("/echo")
def echo_message(payload: MessageRequest):
    return {"received": payload.text}
```

学会标准：

- 知道 FastAPI 应用入口长什么样
- 能看懂路由和函数返回 JSON 的关系
- 能理解 `BaseModel` 是怎么用来接收 JSON 请求体的

### 8. 启动 FastAPI 服务

```bash
uvicorn main:app --reload
```

学会标准：

- 知道 `main:app` 表示 `main.py` 里的 `app`
- 知道 `--reload` 是开发时自动重启

### 9. 在浏览器中访问接口

启动后打开：

```text
http://127.0.0.1:8000/
http://127.0.0.1:8000/health
http://127.0.0.1:8000/docs
```

学会标准：

- 知道 `/` 和 `/health` 是你刚写的接口
- 知道 `/docs` 是 FastAPI 自动生成的接口文档

### 10. 用命令测试 GET 和 POST

新开一个终端窗口后执行：

```bash
curl http://127.0.0.1:8000/
curl http://127.0.0.1:8000/health
curl -X POST http://127.0.0.1:8000/echo \
  -H "Content-Type: application/json" \
  -d '{"text":"hello fastapi"}'
```

学会标准：

- 知道 `GET` 用来获取内容
- 知道 `POST` 可以发送 JSON 数据
- 知道后端是怎么接收并返回结构化数据的

### 11. 学习最基本的 Git 提交

回到仓库根目录后执行：

```bash
cd /Users/ace9264mac/Documents/personal/aia
git status
git add .
git commit -m "docs: add daily checkin and day1 fastapi learning record"
git log --oneline -5
```

学会标准：

- 知道如何查看改动
- 知道如何把今天的学习内容提交到仓库
- 知道如何查看最近提交记录

### 12. 学完后更新今天的打卡状态

学习完成后，把下面内容手动更新到今天这份文档里：

- 把“完成情况”从 `未开始` 改成 `已完成` 或 `进行中`
- 在“结果记录”里写下你实际做到了什么
- 在“今日复盘”里补上今天学会的内容和还不熟的地方

## 今日打卡表

| 模块 | 今日任务 | 学会标准 | 完成情况 | 结果记录 |
| --- | --- | --- | --- | --- |
| Python 环境 | 安装或确认 Python 版本，创建虚拟环境，安装 `fastapi` 和 `uvicorn` | 能独立创建并激活虚拟环境，知道如何安装依赖，并能说清项目依赖放在哪里 | 未开始 | 待记录 |
| Git 基础 | 学会 `status`、`add`、`commit`、`log` 的最基本用法 | 能独立完成一次提交，并知道怎么查看改动和提交历史 | 未开始 | 待记录 |
| HTTP / JSON | 理解请求、响应、状态码、JSON body 的概念 | 能用自己的话解释 `GET` 和 `POST` 的区别，知道接口为什么常用 JSON 传数据 | 未开始 | 待记录 |
| FastAPI 入门 | 写一个最小 FastAPI 项目并成功启动 | 能独立写出 `GET /hello` 接口，并通过浏览器或接口工具访问成功 | 未开始 | 待记录 |
| Pydantic 基础 | 用一个简单模型接收请求参数 | 能写一个最简单的 `BaseModel`，并理解为什么后端要做结构校验 | 未开始 | 待记录 |
| 学习记录 | 更新今日学习文档并写下复盘 | 能明确写出“今天学会了什么、哪里还不熟、明天做什么” | 未开始 | 待记录 |

## 今天学到什么程度算学会

今天不是要求你把 FastAPI 学透，而是达到“能独立跑出第一个 AI 应用后端雏形”的程度。满足下面标准，就算今天学会了：

1. 你能自己创建虚拟环境并安装依赖，不需要重新查完整教程。
2. 你能解释接口请求和 JSON 返回是怎么一来一回的。
3. 你能独立写出一个最简单的 FastAPI 服务并启动它。
4. 你能新增至少一个接口，例如 `/hello` 或 `/health`。
5. 你能完成一次 Git 提交，并把今天的内容记录到仓库里。

## 今日产出

- 一个可运行的 FastAPI Hello World 服务
- 一份今日学习打卡记录
- 至少 1 次 Git 提交

## 遇到的问题

- 问题 1：待记录
- 问题 2：待记录

## 今日复盘

### 今天学会了什么

- 待记录

### 还不熟的地方

- 待记录

### 明天要继续什么

- 明天进入第 2 天内容，继续巩固 FastAPI 接口、请求参数、返回格式，并开始接触模型 API 调用的基本形式
