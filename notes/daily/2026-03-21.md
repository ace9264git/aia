# 2026-03-21 学习打卡

## 基本信息

- 周次：第 1 周
- 日期：2026-03-21
- 学习阶段：第 1 个月，第 1 周
- 今日主题：小型实战，完成一个 Todo API
- 预计投入：4-6 小时
- 建议练习目录：`day6-todo-api/`

## 今日目标

- [ ] 独立搭一个新的 FastAPI 项目
- [ ] 完成 `GET`、`POST`、`PATCH`、`DELETE` 四类基础接口
- [ ] 能用内存列表模拟最简单的数据存储
- [ ] 能通过 `curl` 完整测试一轮接口

## 今日打卡表

| 模块 | 今日任务 | 学会标准 | 完成情况 | 结果记录 |
| --- | --- | --- | --- | --- |
| 项目初始化 | 独立创建新项目和虚拟环境 | 基本不需要参考 Day 1 也能自己搭起来 | 未开始 | 待记录 |
| Todo 创建 | 完成 `POST /todos` | 知道怎么接请求体并生成一条新数据 | 未开始 | 待记录 |
| Todo 查询 | 完成 `GET /todos` | 知道怎么返回列表数据 | 未开始 | 待记录 |
| Todo 状态变更 | 完成 `PATCH /todos/{todo_id}/done` | 知道怎么根据 path 参数修改已有数据 | 未开始 | 待记录 |
| Todo 删除 | 完成 `DELETE /todos/{todo_id}` | 知道怎么根据 id 删除数据并处理不存在情况 | 未开始 | 待记录 |

## 今日实操命令路线

### 1. 创建练习目录

```bash
cd /Users/ace9264mac/Documents/personal/aia
mkdir -p day6-todo-api
cd day6-todo-api
```

### 2. 创建虚拟环境并安装依赖

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install fastapi uvicorn
pip freeze > requirements.txt
```

### 3. 创建 `main.py`

```bash
cat <<'EOF' > main.py
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field

app = FastAPI()
todos = []


class TodoCreate(BaseModel):
    title: str = Field(min_length=1, max_length=100)


@app.get("/todos")
def list_todos():
    return {"items": todos}


@app.post("/todos")
def create_todo(payload: TodoCreate):
    todo = {"id": len(todos) + 1, "title": payload.title, "done": False}
    todos.append(todo)
    return todo


@app.patch("/todos/{todo_id}/done")
def mark_todo_done(todo_id: int):
    for todo in todos:
        if todo["id"] == todo_id:
            todo["done"] = True
            return todo
    raise HTTPException(status_code=404, detail="Todo not found")


@app.delete("/todos/{todo_id}")
def delete_todo(todo_id: int):
    for index, todo in enumerate(todos):
        if todo["id"] == todo_id:
            deleted = todos.pop(index)
            return {"deleted": deleted}
    raise HTTPException(status_code=404, detail="Todo not found")
EOF
```

### 4. 启动服务

```bash
uvicorn main:app --reload
```

### 5. 完整测试 Todo API

```bash
curl http://127.0.0.1:8000/todos
curl -X POST http://127.0.0.1:8000/todos \
  -H "Content-Type: application/json" \
  -d '{"title":"Learn FastAPI"}'
curl -X POST http://127.0.0.1:8000/todos \
  -H "Content-Type: application/json" \
  -d '{"title":"Write notes"}'
curl http://127.0.0.1:8000/todos
curl -X PATCH http://127.0.0.1:8000/todos/1/done
curl -X DELETE http://127.0.0.1:8000/todos/2
curl http://127.0.0.1:8000/todos
```

### 6. 完成后提交

```bash
cd /Users/ace9264mac/Documents/personal/aia
git status
git add .
git commit -m "docs: add week1 day6 learning plan"
git log --oneline -5
```

## 今天学到什么程度算学会

1. 你能不依赖 Day 1 模板重新搭一个项目。
2. 你能自己完成 3-4 个基础接口。
3. 你能用列表和字典模拟最小数据层。
4. 你能根据 id 查询、修改和删除数据。

## 今日产出

- 一个小型 Todo API 项目
- 一组完整跑通的接口测试命令
- 第 1 周最重要的实战产出

## 今日复盘

### 今天学会了什么

- 待记录

### 还不熟的地方

- 待记录

### 明天要继续什么

- 明天做第 1 周复盘、自测，并整理这周到底学会了什么
