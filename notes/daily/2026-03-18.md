# 2026-03-18 学习打卡

## 基本信息

- 周次：第 1 周
- 日期：2026-03-18
- 学习阶段：第 1 个月，第 1 周
- 今日主题：掌握 path 参数、状态码、异常处理和基础校验
- 预计投入：4-6 小时
- 建议练习目录：`day3-fastapi/`

## 今日目标

- [ ] 分清 query 参数和 path 参数
- [ ] 认识 201、404、422 这几个最常见状态
- [ ] 理解 `HTTPException` 的作用
- [ ] 理解 Pydantic 基础校验是怎么工作的

## 今日打卡表

| 模块 | 今日任务 | 学会标准 | 完成情况 | 结果记录 |
| --- | --- | --- | --- | --- |
| Path 参数 | 写一个 `GET /products/{product_id}` 接口 | 知道路径里的参数是怎么传到函数里的 | 未开始 | 待记录 |
| 状态码 | 创建返回 201 的创建接口 | 知道创建资源为什么常返回 201 | 未开始 | 待记录 |
| 异常处理 | 用 `HTTPException` 返回 404 | 知道资源找不到时为什么不能直接返回成功结果 | 未开始 | 待记录 |
| 数据校验 | 给 `name` 和 `price` 增加校验规则 | 知道请求不合法时为什么会得到 422 | 未开始 | 待记录 |

## 今日实操命令路线

### 1. 创建练习目录

```bash
cd /Users/ace9264mac/Documents/personal/aia
mkdir -p day3-fastapi
cd day3-fastapi
```

### 2. 创建虚拟环境并安装依赖

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install fastapi uvicorn
pip freeze > requirements.txt
```

### 3. 创建 `main.py`

```bash
cat <<'EOF' > main.py
from fastapi import FastAPI, HTTPException, status
from pydantic import BaseModel, Field

app = FastAPI()
products = []


class ProductCreate(BaseModel):
    name: str = Field(min_length=2, max_length=30)
    price: float = Field(gt=0)


@app.get("/products")
def list_products():
    return {"items": products}


@app.get("/products/{product_id}")
def get_product(product_id: int):
    for product in products:
        if product["id"] == product_id:
            return product
    raise HTTPException(status_code=404, detail="Product not found")


@app.post("/products", status_code=status.HTTP_201_CREATED)
def create_product(payload: ProductCreate):
    product = {"id": len(products) + 1, **payload.model_dump()}
    products.append(product)
    return product
EOF
```

### 4. 启动服务

```bash
uvicorn main:app --reload
```

### 5. 测试正常请求

```bash
curl http://127.0.0.1:8000/products
curl -X POST http://127.0.0.1:8000/products \
  -H "Content-Type: application/json" \
  -d '{"name":"Keyboard","price":199}'
curl http://127.0.0.1:8000/products/1
```

### 6. 测试异常和校验

```bash
curl http://127.0.0.1:8000/products/999
curl -X POST http://127.0.0.1:8000/products \
  -H "Content-Type: application/json" \
  -d '{"name":"A","price":0}'
```

### 7. 观察接口文档

```text
http://127.0.0.1:8000/docs
```

### 8. 完成后提交

```bash
cd /Users/ace9264mac/Documents/personal/aia
git status
git add .
git commit -m "docs: add week1 day3 learning plan"
git log --oneline -5
```

## 今天学到什么程度算学会

1. 你知道 `/products/1` 里的 `1` 会进到 `product_id`。
2. 你知道创建成功通常可以返回 201。
3. 你知道找不到资源时可以抛 `HTTPException`。
4. 你知道 Pydantic 校验失败后，FastAPI 会返回 422。

## 今日产出

- 一个带 path 参数的 FastAPI 练习项目
- 一个能返回 201、404、422 的练习接口
- 对状态码和校验的第一轮理解

## 今日复盘

### 今天学会了什么

- 待记录

### 还不熟的地方

- 待记录

### 明天要继续什么

- 明天进入项目结构、router 和环境变量基础
